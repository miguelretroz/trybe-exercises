const errorHandler = require('./errorHandler');
const firstNameHandler = require('./firstNameHandler');
const lastNameHandler = require('./lastNameHandler');
const emailHandler = require('./emailHandler');
const passwordHandler = require('./passwordHandler');

module.exports = {
  errorHandler,
  firstNameHandler,
  lastNameHandler,
  emailHandler,
  passwordHandler,
};